streamlit>=1.40.0
python-dotenv>=1.0.0
pandas>=2.0.0
openai>=1.55.0
# Observabilidade LLM (traces, sessões, custo por chamada).
langfuse>=3.0.0,<5.0.0
txtai>=9.0.0
sentence-transformers>=3.0.0
python-docx>=1.1.0
openpyxl>=3.1.0
pypdf>=5.0.0
duckdb>=1.5.0,<2.0.0
flaml[automl]>=2.1.0,<3.0.0
scikit-learn>=1.3.0
joblib>=1.3.0
PyYAML>=6.0.0
kagglehub[pandas-datasets]>=0.3.0
# ESM-2 (embeddings de sequência). CPU: pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.2.0,<3.0.0
# ESM-2: versões 4.40–4.47 podem falhar com token_dropout em lote; 4.48+ ou 5.x é mais seguro.
transformers>=4.48.0,<6.0.0
# Multiagentes — CrewAI 1.14+ com extra openai (OpenRouter é OpenAI-compatible).
# Hoje só usado para o builder em ``agents/llm.py`` (reservado); o pipeline custom
# em ``agents/crew.py`` não chama ``crewai.Crew``. Manter por compat futura.
crewai[openai]>=1.14.0,<2.0.0
